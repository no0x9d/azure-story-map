name: Build Executables

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    strategy:
      matrix:
        include:
          - os: macos
            target: macos-arm64
            artifact-name: azsm
          - os: windows
            target: windows-x64
            artifact-name: azsm.exe
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1

      - name: Install dependencies
        run: bun install

      - name: Build application
        run: APP_NAME=${{ matrix.artifact-name }} APP_TARGET=${{ matrix.target }} bunx --bun vite build

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: azsm-${{ matrix.target }}
          path: ./dist/${{ matrix.artifact-name }}
          retention-days: 30

      - name: Upload file to Release
        if: github.ref_type == 'tag'
        uses: softprops/action-gh-release@v1
        with:
          files: |
            ./dist/*
